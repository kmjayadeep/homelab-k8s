apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deluge-data
  namespace: deluge
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
