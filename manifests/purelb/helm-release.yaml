# apiVersion: helm.toolkit.fluxcd.io/v2beta1
# kind: HelmRelease
# metadata:
  # name: purelb
  # namespace: purelb
# spec:
  # interval: 5m
  # chart:
    # spec:
      # chart: purelb
      # version: 'v0.6.1'
      # sourceRef:
        # kind: HelmRepository
        # name: purelb
        # namespace: flux-system
      # interval: 1m
  # values: {}
