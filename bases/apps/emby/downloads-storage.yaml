# Create a persistent volume to mount local downloads folder into container
apiVersion: v1
kind: PersistentVolume
metadata:
  name: downloads-pv-emby
  labels:
    volume-type: downloads-volume
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  claimRef:
    kind: PersistentVolumeClaim
    name: downloads-pvc-emby
    namespace: emby
  storageClassName: downloads-storage-jf
  hostPath:
    path: /mnt/downloads
    type: Directory
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-pvc-emby
spec:
  storageClassName: downloads-storage-jf
  volumeName: downloads-pv-emby
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
