apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dashy

configMapGenerator:
- name: dashy-config
  files:
    - assets/config.yml

resources:
  - ./deploy.yaml
  - ./svc.yaml
  - ./ing.yaml
