apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homer

configMapGenerator:
- name: homer-config
  files:
    - assets/config.yml

resources:
  - ./namespace.yaml
  - ./deploy.yaml
  - ./svc.yaml
  - ./ing.yaml
