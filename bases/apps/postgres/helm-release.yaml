apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: postgres
  namespace: postgres
spec:
  chart:
    spec:
      chart: postgres
      version: "12.6.8"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: postgres
  interval: 1h0m0s
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  values: {}
