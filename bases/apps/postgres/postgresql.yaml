apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: cosmos
spec:
  teamId: "acid"
  volume:
    size: 1Gi
  numberOfInstances: 1
  users:
    planka-user:  # database owner
    - superuser
    - createdb
  databases:
    planka: planka-user  # dbname: owner
  preparedDatabases:
    bar: {}
  postgresql:
    version: "15"
