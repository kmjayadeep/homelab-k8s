apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: vault
  namespace: vault
spec:
  chart:
    spec:
      chart: vault
      version: "0.26.0"
      sourceRef:
        kind: HelmRepository
        name: hashicorp
        namespace: vault
  interval: 1h0m0s
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
