apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/instance: planka
    app.kubernetes.io/name: planka
  name: planka
  namespace: productivity
spec:
  storageClassName: longhorn-static
  volumeMode: Filesystem
  volumeName: longhorn-planka-data
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
