apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bookorbit

resources:
  - app-deploy.yaml
  - postgres-deploy.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
  - secret-sealed.yaml
