apiVersion: v1
kind: Service
metadata:
  name: dotbintask
spec:
  selector: 
    app: dotbintask
  type: ClusterIP
  ports:
  - name: api
    port: 8080
    targetPort: 8080
  - name: frontend
    port: 80
    targetPort: 80