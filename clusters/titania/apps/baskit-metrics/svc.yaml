apiVersion: v1
kind: Service
metadata:
  name: baskit-metrics
  labels:
    app: baskit-metrics
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: baskit-metrics

