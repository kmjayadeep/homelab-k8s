apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kserve-localmodel-resources
spec:
  interval: 1h
  timeout: 10m
  chartRef:
    kind: OCIRepository
    name: kserve-localmodel-resources
  dependsOn:
    - name: kserve-resources
    - name: kserve-localmodel-crd
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  postRenderers:
    - kustomize:
        patches:
          - target:
              group: apps
              version: v1
              kind: DaemonSet
              name: kserve-localmodelnode-agent
            patch: |-
              - op: add
                path: /spec/template/spec/containers/0/securityContext/runAsUser
                value: 1000
              - op: add
                path: /spec/template/spec/containers/0/securityContext/runAsGroup
                value: 1000
              # The AI node is intentionally tainted workload=ai:NoSchedule.
              # Permit the cache node agent to run there so LocalModelCache
              # downloads can complete before predictors are scheduled there.
              - op: add
                path: /spec/template/spec/tolerations
                value:
                  - key: workload
                    operator: Equal
                    value: ai
                    effect: NoSchedule
  values:
    kserve:
      localmodel:
        enabled: true
